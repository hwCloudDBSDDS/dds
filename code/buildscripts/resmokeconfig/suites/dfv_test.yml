selector:
  js_test:
    roots:
    - jstests/dfv_test/test1.js

executor:
  js_test:
    config:
      shell_options:
        readMode: commands
    fixture:
      class: ShardedClusterFixture
      mongos_options:
        set_parameters:
          enableTestCommands: 1
      mongod_options:
        set_parameters:
          enableTestCommands: 1
          numInitialSyncAttempts: 1
      enable_sharding:
      - test
